# Base URL of the FastAPI backend
VITE_API_BASE_URL=http://127.0.0.1:8000

# true  -> serve all data from src/mocks (no backend needed)
# false -> hit VITE_API_BASE_URL using the paths in src/store/api/endpoints.ts
VITE_USE_MOCKS=true
